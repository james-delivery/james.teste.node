service:
  name: typescript-serveles-pagarme

plugins:
  - serverless-plugin-typescript
  - serverless-plugin-optimize
  - serverless-offline

provider:
  name: aws
  runtime: nodejs12.x

functions:
  process:
    handler: src/handler.process
    events:
      - schedule:
          rate: rate(1 minute)
